<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof pageTitle !== 'undefined' ? pageTitle : 'Lizzyland — Where Pipe-dreams Come True' %></title>
  <meta name="description" content="<%= typeof pageDesc !== 'undefined' ? pageDesc : 'Lizzyland — your instant internet space, tropical lifestyle store, and pipe-dream launcher.' %>">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400&family=DM+Sans:wght@300;400;500&family=Cormorant+Garamond:ital,wght@1,400;1,600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/lizzyland.css">
</head>
<body>

  <!-- ══ HEADER ══ -->
  <header class="ll-header">
    <div class="ll-header-inner">

      <a href="/" class="ll-logo">
        <img src="/images/lizzyland-logo.png" alt="Lizzyland" class="ll-logo-img">
        <span class="ll-logo-text">Lizzy<span>land</span></span>
      </a>

      <!-- Mobile hamburger -->
      <button class="ll-nav-toggle" id="navToggle" aria-label="Open menu">☰</button>

      <nav class="ll-nav-wrap" id="navWrap">
        <ul class="ll-nav">
          <li><a href="/" class="<%= typeof currentPage !== 'undefined' && currentPage === 'home' ? 'active' : '' %>">Home</a></li>
          <li><a href="/shop" class="<%= typeof currentPage !== 'undefined' && currentPage === 'shop' ? 'active' : '' %>">Shop</a></li>
          <li><a href="/space" class="<%= typeof currentPage !== 'undefined' && currentPage === 'space' ? 'active' : '' %>">Your Internet Space</a></li>
        </ul>
        <a href="/shop" class="ll-nav-cta">🛍️ Shop Now</a>
      </nav>

    </div>
  </header>
